<nav>
        <ul>
            <div class="barreNavigation">
            {% if app.user %}
                {% if app.user.administrateur %}
            <li><a href="{{ path('profile_list') }}">Liste des élèves</a></li>
            <li><a href="{{ path('ville_list') }}">Villes</a></li>
            <li><a href="{{ path('campus_list') }}">Campus</a></li>
                {% endif %}
            <li><a href="{{ path('main_homepage') }}">Accueil</a></li>
            <li><a href="{{ path('profile_index') }}">Mon profil</a></li>
            <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>
            {% endif %}
    {#        {% if app.user %}#}
    {#            <li><a href="{{ path('serie_add') }}">New TV show</a></li>#}
    {#            <li><a href="{{ path('app_logout') }}">Logout ({{ app.user.firstname }})</a></li>#}
    {#        {% else %}#}
    {#            <li><a href="{{ path('app_login') }}">Login</a></li>#}
    {#            <li><a href="{{ path('app_register') }}">Register</a></li>#}
    {#        {% endif %}#}
            </div>
        </ul>
</nav>
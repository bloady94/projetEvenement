{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Campus

{% endblock %}

{% block body %}
    <h2>Liste des campus</h2>

    {% for camp in campus %}
        <p>{{ camp.nom }}</p>
    {% endfor %}

{% endblock %}

